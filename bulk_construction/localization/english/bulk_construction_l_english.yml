l_english:
 BC_PROBE_FIRE_BUTTON:0 "BC probe: fire"
 BC_PROBE_FIRE_BUTTON_TOOLTIP:0 "Probe build. This builds nothing. It writes one line to debug.log per state this panel currently lists as valid, so the iteration can be counted."
